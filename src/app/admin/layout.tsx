export const metadata = {
  title: 'Sanity Studio | Gurkha Health & Services',
  description: 'Admin dashboard for managing content',
};

export default function AdminLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
